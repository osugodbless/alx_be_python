# Define the variables required for conversion
hours = 2

# Calculate the number of seconds per given hours
seconds = hours * 3600          # 1 hour is equal to 3600 seconds

# Print the result of the conversion
print(hours, "hour(s) is", seconds, "seconds")
